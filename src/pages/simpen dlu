<!-- import { useParams } from "react-router";
import data from "../data/data.json";

export default function CourseDetail() {
  const { id } = useParams();

  const course = data.find((item) => item.id === Number(id));

  if (!course) {
    return <h1>Course not found</h1>;
  }

  return (
    <div className="min-h-screen bg-gray-50 py-12 px-6">
      <div className="max-w-4xl mx-auto bg-white rounded-2xl shadow-lg p-8 grid md:grid-cols-2 gap-8">
        
        {/* Gambar */}
        <div>
          <img
            src={course.image}
            alt={course.title}
            className="w-full rounded-xl"
          />
        </div>

        {/* Detail */}
        <div>
          <h1 className="text-3xl font-bold mb-4">
            {course.title}
          </h1>

          <p className="text-gray-600 mb-2">
            Level: {course.level}
          </p>

          <p className="mb-2">
            {course.lessons} lessons
          </p>

          <p className="mb-6">
            Duration: {course.duration}
          </p>

          <button className="bg-black text-white px-6 py-2 rounded-lg hover:bg-gray-800 transition">
            Start Course
          </button>
        </div>

      </div>
    </div>
  );
} -->

_____________________________________________________________________________________



<!-- export default function Home() {

  // 1️⃣ Huruf yang mau ditampilin
  // Kamu tadi mau: m,o,n,o,s,t,r,k
  const letters = [
    { char: "M", top: "10%" },
    { char: "O", top: "60%" },
    { char: "N", top: "25%" },
    { char: "O", top: "80%" },
    { char: "S", top: "40%" },
    { char: "T", top: "15%" },
    { char: "R", top: "65%" },
    { char: "K", top: "30%" },
  ];

  return (
    // 2️⃣ Background halaman
    <div className="min-h-screen bg-white overflow-hidden">

      {/* 3️⃣ Jumlah garis vertikal */}
      {/* grid-cols-8 karena ada 8 huruf */}
      <div className="grid grid-cols-8 h-screen">

        {letters.map((item, i) => (
          <div
            key={i}

            // 4️⃣ Garis vertikal
            className="relative border-r border-gray-300"
          >

            <span
              // 5️⃣ Posisi huruf di garis (atas bawah)
              style={{ top: item.top }}

              // 6️⃣ Styling huruf
              className="
                absolute 
                left-1/2 
                -translate-x-1/2
                text-[10rem]     /* ukuran huruf */
                font-black       /* ketebalan */
                font-mono-me     /* font kamu */
                text-black       /* warna */
                leading-none
              "
            >
              {item.char}
            </span>

          </div>
        ))}

      </div>

    </div>
  );
} -->